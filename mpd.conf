# Unix socket only: clients on it may add file:// URIs, so no music_directory/db.
bind_to_address "/tmp/radio-mpd.sock"
pid_file        "/tmp/radio-mpd.pid"
log_file        "/tmp/radio-mpd.log"
log_level       "notice"

audio_output {
	type "osx"   # "alsa" on Linux
	name "default"
}
